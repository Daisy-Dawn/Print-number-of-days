#include <stdio.h>
#include <stdlib.h>

int main(void) 
{
char month[20];
  printf("Welcome user, please enter the month of the year:\t");
  scanf("%s", &month);

  if (month == "January" || month == 'March' || )
  
  printf("\nEnd of program\n\n");
  sleep (5);
  system("clear");
  printf("Welcome user,");
  
  return 0;
}